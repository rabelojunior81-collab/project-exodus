{
  "name": "@project-exodus/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Three.js 3D PBR Web Client with Isometric RTS camera and HUD",
  "scripts": {
    "predev": "npm --prefix .. run build:shared",
    "dev": "vite",
    "prebuild": "npm --prefix .. run build:shared",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "node --version"
  },
  "dependencies": {
    "@project-exodus/shared": "*",
    "three": "^0.174.0"
  },
  "devDependencies": {
    "@types/three": "^0.174.0",
    "typescript": "^5.8.2",
    "vite": "^6.2.0"
  }
}
