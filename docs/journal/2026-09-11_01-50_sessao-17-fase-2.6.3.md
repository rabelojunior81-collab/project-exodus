# Sessão 17 — Fase 2.6.3 (Reconciliação) + Camada de Evidência Visual + Landing Narrativa

> **Ciclo**: diretiva do usuário — "complete a sub-fase 2.6.3" com três diretivas novas:
> (1) registro visual de testes com manifesto por screenshot; (2) testes expandidos **antes** da
> implementação (contrato congelado, sem afrouxar depois); (3) narrativa viva na landing a cada
> fechamento. Máxima: "Organização é lindo! Registre tudo: Antes, durante, depois e sempre!"
> **Data/Hora**: 2026-09-11T01:50:00-03:00
> **Escopo**: `shared/` (collision, física, economia, protocolo v3), `server/`, `client/`,
> `docs/evidence/` (novo), `tools/visual-check/` (lib + gates), `landing/`.

---

## 0. Sumário Executivo

A reconciliação decidida no grill-me foi **entregue com contrato congelado**: os testes que definem
a 2.6.3 foram escritos e registrados **em vermelho antes de qualquer código** e passaram verdes
**sem alteração**. O servidor agora coleta a 3,33 un/s, move com física inercial (o blindado gira
antes de andar de ré) e projeta colisão pelo **mesmo resolvedor do cliente** (`shared/collision`),
com dropoff de 10 m e mundo limitado a ±88 m.

Duas diretivas estruturais nasceram junto: a **camada de evidência visual** (`docs/evidence/` com
manifesto por captura, status aplicado/validado/passou/falhou, comando e commit — integrada à
governança como irmã do journal/specs) e a **narrativa viva da landing** (seção "Crônicas da
Construção", atualizada a cada fechamento, começando pelas capturas desta fase).

**Suíte: 65 asserts. Dois bugs de física foram pegos pelos próprios testes congelados** antes de
qualquer evidência verde — exatamente o propósito da diretiva.

## 1. O Que Foi Entregue

### 1.1 Contrato congelado (antes da implementação)

- `server/src/__tests__/parity.test.ts` — 9 asserts: intervalo de coleta 6 ticks/3,33 un/s;
  10 de carga em **60 ticks exatos**; dropoff 10 m com entrega a ≤ 10,5 m; stats de física por
  tipo; inércia (0,25 m/s no 1º tick; ≥ 6 m/s após 2 s); tração (ré < 0,7 m em 1 s); snapshot v3;
  clamp ±88; MOVE para fora do mapa termina ≤ 88.
- `server/src/__tests__/collision-shared.test.ts` — 5 asserts: projeção/bloqueio exato,
  determinismo, projeção de destino interno, derivação das constantes e uso do resolvedor único.
- `tools/visual-check/phase-2.6.3-e2e.mjs` — E2E congelado com captura de evidência e invariantes
  do cliente (chegada ao veio, colisão, coleta E2E, 0 pageerrors).
- **Estado vermelho registrado** (`docs/evidence/fase-2.6.3/t-00-suite-do-servidor/00-red.txt`):
  `AssertionError: 10 !== 6` — o contrato falhando antes do código existir.

### 1.2 Camada de evidência visual (governança)

- `docs/evidence/README.md` — convenções, esquema do manifesto, vocabulário de status e regras de
  ouro (incluindo "testes congelados primeiro" e "captura fiel").
- `docs/evidence/fase-2.6.3/MANIFEST.md` — índice da fase com marcadores para upsert automatizado.
- `tools/visual-check/lib/evidence.mjs` — registry reutilizável (screenshot/texto → PNG/TXT +
  `*.manifest.json` + merge no índice por id).
- `tools/visual-check/server-suite-evidence.mjs` — registra a suíte do servidor (transcrição +
  status) como evidência; usado em `--label red` e `--label green`.

### 1.3 Reconciliação (2.6.3)

- **Shared**: `collision.ts` (resolvedor único + constantes derivadas), `WORLD_LIMIT=88` +
  `clampToWorld` unificado, `GATHER_INTERVAL_TICKS=6`, `DROPOFF_RANGE=10`, stats de física
  (acceleration/rotationSpeed) por tipo, snapshot **v3** com `velocity`/`heading`,
  `GridObstacle` (grid) separado de `CircleObstacle` (colisão) no barrel.
- **Servidor**: física inercial em `advance()` (um alinhamento por tick; orçamento `v×dt`;
  tração por alinhamento; encaixe de waypoints), projeção via `CollisionWorld` do shared,
  cenário com tesouro (S16) inalterado; `LEGACY` de coleta/dropoff eliminados.
- **Cliente**: `collision.ts` virou wrapper (re-exporta o shared + declive local); física lida do
  shared no `unit.ts`. Zero mudança de comportamento visível — provado pelos gates.

### 1.4 Landing narrativa (diretiva 3)

- Seção **"Crônicas da Construção"** (`#cronicas`): 4 entradas canônicas (S13, S14, S15–16, S17)
  com capturas de evidência convertidas para WebP (~150 KB cada) em `landing/assets/evidence/`.
- Chip do roadmap da 2.6 atualizado para "Em curso"; convenção de atualização documentada no
  `landing/README.md` (imagem da evidência + texto PT/EN; multimodal quando houver áudio/vídeo).

## 2. Gates Executados Nesta Sessão (evidência)

| # | Gate | Resultado |
| :-- | :--- | :--- |
| G1 | Typechecks ×4 | 🟢 0 erros |
| G2 | Contrato vermelho registrado | 🟢 `10 !== 6` em `docs/evidence/.../00-red.txt` |
| G3 | Suíte do servidor (verde) | 🟢 smoke + **65 asserts** (contrato 2.6.3: 9+5) |
| G4 | Suíte verde registrada | 🟢 `00-green.txt` + manifesto `passou` |
| G5 | E2E congelado + capturas | 🟢 `phase-2.6.3-e2e` OK · 5 capturas + manifestos + transcrição |
| G6 | Produção | 🟢 `dist-proof` 11/8/0 · carimbo do build |
| G7 | HUD | 🟢 `test-buttons` 10/10 + coleta E2E |
| G8 | Build | 🟢 28 módulos · 689,29 kB (gzip 182,36) |

**Dois bugs de física pegos pelo contrato** (registrados para memória):
1. Orçamento de tick composto (o loop movia `speed×dt` em passos de `v×dt`) → fix para um
   alinhamento + orçamento `v×dt` por tick.
2. Waypoint degenerado (célula de origem) congelava a unidade na intenção → skip antes do cálculo.

## 3. Decisões de Implementação (S17)

| # | Decisão |
| :-- | :--- |
| D-17.1 | Contrato congelado em arquivos próprios (`parity`/`collision-shared`), imunes a ajustes pós-implementação |
| D-17.2 | Evidência em `docs/evidence/` com manifesto por captura + índice por fase (upsert por id) |
| D-17.3 | `GridObstacle` (A*) ≠ `CircleObstacle` (colisão) — nomes que dizem o domínio |
| D-17.4 | Física: 1 alinhamento/tick + orçamento `v×dt` (determinístico e estável) |
| D-17.5 | Cliente consome o shared sem mudar comportamento (gates provam); wrapper fino preserva a API |
| D-17.6 | Landing: crônica por fechamento com asset da evidência (convenção no README) |

## 4. Pendências

1. **2.6.4** — broadcast por tick (full JSON 20 Hz, D-2.6.4-A) + `viewFor(player)` (D-2.6-D).
2. **2.6.5** — cliente WS com buffer ~100 ms (D-2.6.5-A), RTT instrumentado, hooks dev-only (D-2.6.5-B).
3. **2.6.6** — remoção do legado client-side + auditoria.
4. ALTO-05 (música/áudio), CI de gates, topics/social preview, Lote 3 eras 2–4.

---
*Registro assinado por:*
- **Harness/Agente**: Kilo CLI
- **Modelo LLM**: deepseek-v4.1-flash
- **Timestamp**: 2026-09-11T01:50:00-03:00
