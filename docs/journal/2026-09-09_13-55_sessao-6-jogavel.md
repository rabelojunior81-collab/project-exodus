# Diário de Bordo — 2026-09-09 13:55 — Sessão 6 (Playtest Furioso → Jogo Jogável)

> **Ciclo**: Sessão 6 — usuário detonou com razão (HUD, minimapa, zoom, luz, botões mortos)
> **Status**: Fase 1.8 fechada, tudo verificado em dev E em produção 🟢

---

## 1. A causa raiz nº 1: `dist/` com 9 horas de atraso

`client/dist/index.html` datado de 04:01 contra fontes de 12:29–13:16. O usuário via o jogo velho (fog soup, zoom 18–80, HUD antigo, botões mortos) — "não mudou nada" era literalmente verdade. Rebuild (`tsc && vite build`) + prova via `vite preview` (shot 08: 9 entidades, 0 erros). Lição permanente: **todo turno que toca client/ termina com rebuild + prova de dist**.

## 2. O que foi consertado (tudo pelo orquestrador, sem delegação)

- **Zoom**: frustum 18–80 → 9–110, zoomSpeed 0.15, bounds ±80, `setTarget` com Y do relevo (colinas centralizadas).
- **Minimap**: bugs reais — overlay sem `pointer-events:none` comia cliques da faixa inferior; handler duplicado main+hud (fonte única agora via `bindMinimap`). Upgrade: thumbnail do relevo pré-renderizada (rampa de altura), right-click = ordem + ping + evento. Teste: MINIMAP_CLICK 53.5≈54.
- **Luz**: névoa 0.011→0.0032 (a sopa), hemisfério céu-terra, sol mais alto, exposição 1.55, anéis opacos.
- **Legibilidade**: HP bars 3D (sprite canvas, sempre visíveis, verde→vermelho) + `updateHealth()` pronto p/ combate.
- **Botões reais**: Mover (modo), Parar, Patrulha (2 cliques, ping-pong), Reunião, Dispersar, Recrutar (fila local c/ custo/tempo/pop, reembolso no cancel, spawn no rally), Retrato clica-seleciona. Ataque/defesa: feed honesto "Fase 5". Economia local marcada TODO-2.6 (servidor assume).
- **Patrulha**: `setPatrol(a,b)` na Unit, chegada alterna o ponto.

## 3. Verificação

tsc 0 (dev e build), harness full (MOVE_CHECK ancora tudo, MINIMAP_CLICK novo), shots 01/02 revisados (legível, HP bars, minimapa com terreno, badge de eventos "2"), shot 08 prova de produção, mobile 07 intacto (sem regressão).

## 4. Follow-ups

1. Treinamento de RUST_RAIDER/SCRAP_BUGGY além do Catador (botão Recrutar hoje = Catador; UI de escolha por edifício).
2. Limpar highlight `order-active` após resolver a ordem (markActiveOrder é privado).
3. Coalescência de eventos (A1) e viewport mobile permanente no check.mjs.
4. Sub-fase 2.6 (integração server) quando o usuário aprovar o playtest.

---
*Registro assinado por:*
- **Harness/Agente**: opencode
- **Modelo LLM**: Muse Spark (muse-spark)
- **Timestamp**: 2026-09-09T13:55:00-03:00
