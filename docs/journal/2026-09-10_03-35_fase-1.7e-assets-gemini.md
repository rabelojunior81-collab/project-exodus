# Journal — 2026-09-10 03:35 — Fase 1.7E: Assets Gemini (Lotes 1, 2 e piloto 3)

## Contexto
Fase 1.7 Expandida aprovada pelo usuário na Sessão 9; 1.7A–D já fechadas. Restava 1.7E (assets Gemini).

## O que foi feito

### Tooling
- `tools/studio-gemini/package.json`: scripts migrados de `--loader ts-node/esm` para `tsx`.
- `src/client.ts`: chave via header `x-goog-api-key`, modelo texto em `GEMINI_TEXT_MODEL`, timeout + retry/backoff, log de tokens/custo.
- `tsconfig.json`: inclui `tests/`.
- Criados `src/generators/portraits.ts`, `tts.ts`, `music.ts`, `video.ts` com cache-first, `.meta.json`, `--dry-run`.
- `src/generators/_shared.ts`: `resolveClientAssets()` reescrito para detectar raiz do projeto via `client/package.json` + `package.json` — corrige o bug que salvou arquivos em `tools/studio-gemini/client/`.
- Adicionados `sharp`, `tsx`, `ffmpeg-static` como devDeps do workspace.

### Lote 1 — Retratos e ícones
- 17 WebP 512×512 gerados (heróis das 3 facções, 4 eras, bunker-gunner, drone, mech, buggy, command-center) + thumbs 128px + `.meta.json`.
- 4 SVGs de recursos em `client/public/assets/icons/`.
- JPGs legados mantidos como fallback; `hud.ts` atualizado para preferir `.webp`.

### Lote 2 — TTS e áudio real
- 16 MP3 gerados: 4 briefings de era (voz Charon) + 10 voice-lines (Kore/Puck) + 2 alertas (Charon).
- API TTS retorna `audio/L16` PCM 24kHz; gerador converte via `ffmpeg-static` para MP3 64 kbps mono 22,05 kHz + OGG fallback quando possível.
- `client/src/engine/audio.ts` implementado: WebAudio com buses SFX/Voice/Music, unlock no primeiro gesto, cache de buffers, `playSelect`, `playCommand`, `playEffect`, `playBriefing`.
- Fiação: select/move/gather/deposit/briefing disparam áudio; fallback Oscillator se arquivo ausente.

### Piloto Lote 3 — Música e vídeo
- Dry-run realizado: modelos `lyria-002` e `veo-3.1-generate-preview` listados.
- Chamadas reais retornaram **HTTP 404** (modelos não encontrados na API `v1beta` para `generateContent` nesta conta/região).
- Decisão: não gerar placeholders falsos; registrar fallback procedural (loops WebAudio + Ken-Burns sobre retratos) em `ATTRIBUTION.md` e `ASSET_PLAN.md`.

### Governança de assets
- `client/public/assets/MANIFEST.md` regenerado: 75 arquivos publicados, 18,02 MB total.
- `client/public/assets/ATTRIBUTION.md` criado com origem dos GLBs, retratos/áudio gerados por Gemini e nota sobre Lote 3.
- `tools/studio-gemini/ASSET_PLAN.md` atualizado com custo real aproximado.

## Gates
- `npm run build` (client/server/gemini): OK.
- `npx tsc --noEmit` client/server: 0 erros.
- `tools/visual-check/dist-proof.mjs`: 11 entidades, 8 nós, 0 pageerrors.
- `tools/visual-check/test-buttons.mjs`: 10/10 PASS; gather-e2e PASS.
- Áudio validado via `file`: MP3 MPEG ADTS layer III 64 kbps 22,05 kHz mono.

## Desvios e correções de processo
- Subagente da 1.7E estourou timeout de 2h após gerar retratos/ícones/TTS parcialmente; áudio foi salvo em pasta errada (`tools/studio-gemini/client/`) por causa da heurística de `resolveClientAssets()`.
- Orquestrador copiou os arquivos válidos para `client/public/assets/audio/`, regenerou apenas os 3 MP3 ausentes, deletou a pasta errada e reescreveu a função de resolução de caminho.
- `ffmpeg-static` exigiu `createRequire(import.meta.url)` no módulo ESM; `require` global não está disponível em arquivos `.ts` ESM.

## Próximo
Fase 2.6 — integração cliente↔server.

---
*Registro assinado por:*
- **Harness/Agente**: Kimi Code CLI
- **Modelo LLM**: Kimi
- **Timestamp**: 2026-09-10T03:35:00-03:00
