# Journal — 2026-09-09 23:35 — Fase 1.7D: Mineração viva (retomada pós-interrupção)

## Interrupção e retomada
O subagente da 1.7D (Kimi Code) foi cancelado às 22:54 com `particles.ts` recém-criado e edições parciais em `unit.ts`/`main.ts`/`audio.ts`. A retomada às 22:56 morreu com `[provider.api_error] 400 (Console Go): Request is missing x-opencode-session` — o OpenCode Go exige header de sessão desde 05/09 e o Kimi Code ainda não o envia nativamente (MoonshotAI/kimi-code#3506, fechada como *not planned*).

**Correção do provider (fora do repo)**: `[providers.opencode-go.custom_headers]` com `x-opencode-session = <uuid estável>` em `~/.kimi-code/config.toml` (sintaxe validada no binário: precedência `env < host < custom_headers`; `kimi doctor` OK; request real de teste respondeu). Nota: o ID é estável por instalação, não por conversa — o Kimi Code não suporta templating dinâmico por sessão.

## O que foi feito (conclusão da 1.7D)
- **`fx/particles.ts`**: type error do `spawn` corrigido (campo `gravity` removido da API por não ter uso por-burst); `setEmitter` passou a atualizar emissores existentes **preservando o acumulador** (chamadas por frame zeravam o rate); material dos pontos corrigido para câmera ortográfica (`size: 5` em pixels, `sizeAttenuation: false`) — com o tamanho em unidades as partículas ficavam sub-pixel e invisíveis.
- **`main.ts`**: bloco de mineração viva no loop — `harvestedNodeIds` a partir de `unit.harvestNodeId`, `active` exige célula visível (`fogOfWar.isVisibleAt`), pulso emissivo lerpado (0.5 ± 0.35) nos `pulseMats` do veio, emissor de faíscas a +1.7m (não ocluído pelas caixas), `particleFx.update(delta)`; entrega em `deposit()` com burst (18 partículas), texto flutuante `+10 <RECURSO>` e `TacticalAudio.playEffect('deposit')`; flash da topbar garantido via `refreshTopbar()` (já existia em `hud.setResources`).
- **Harness**: `debugSetNodeAmount` no `__rts` para fotografar os estágios de depleção; janela do mech no `test-buttons.mjs` 120s→240s.

## Gates (evidências)
- tsc client/server 0.
- Shots `17d-deplete-100/50/15/0` (4 estágios distintos), `17d-harvest-fx` (diagnóstico no browser: `pulse=0.71`, `poolAlive=9`), `17d-delivery-burst` (`+10 SUCATA` + flash `+10` na topbar).
- Fog gate: `node_conc_2` (nunca explorado) sem anel/faíscas — `isExploredAt=false`.
- test-buttons 10/10; gather-e2e PASS (veio 1500→1490, sucata 180→190); dist-proof n=11, 0 pageerrors; 0 pageerrors nos gates visuais.

## Decisões/desvios
- Faíscas nascem a +1.7m do solo (spec dizia "ponto de contato"; a 1.2m ficavam dentro/atrás das caixas na câmera isométrica).
- Texto da primeira entrega passou de `key.toUpperCase()` para `KIND_LABEL` (`RAÇÕES` etc.) — consistência com o texto flutuante.
- Falha intermitente do clique real no veio quando o worker já está dentro de `GATHER_RANGE`: o harness usa fallback `setGather`; o fluxo real foi coberto pelo gather-e2e (PASS).
- Áudio permanece no-op documentado (TODO 1.7E), conforme o escopo.

---
*Registro assinado por:*
- **Harness/Agente**: opencode
- **Modelo LLM**: DeepSeek V4 Flash Vision Exp
- **Timestamp**: 2026-09-09T23:35:00-03:00
